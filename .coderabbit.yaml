# CodeRabbit configuration: https://docs.coderabbit.ai/reference/configuration
reviews:
  # Request changes while review comments remain open.
  # Approve the pull request after all comments are resolved.
  request_changes_workflow: true
  # Skip status comments. The check run carries that signal.
  review_status: false
  auto_review:
    enabled: true
